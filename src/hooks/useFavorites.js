import { useEffect, useState } from 'react';
import ftn from '../api/ftn';
import AsyncStorage from '@react-native-community/async-storage';


export default () => {

    return [userid, getuserid, results, getFavorites]
}