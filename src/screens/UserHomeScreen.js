import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const UserHomeScreen = () => {
    return <Text style={{ fontSize: 48 }}>User Home Screen</Text>
};

const styles = StyleSheet.create({});

export default UserHomeScreen;